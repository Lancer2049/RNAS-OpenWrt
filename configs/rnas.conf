# RNAS Global Configuration
# This file sets global defaults for the RNAS NAS platform.

[global]
enabled = yes
# Service startup: individual services controlled via systemd rnas.target
log_level = info
config_dir = /etc/rnas
run_dir = /var/run/rnas
